# 第一次：粗提取。
# 获取输入，进行分词，从索引表中提取出存在分词的案件的id
# 输入：从前端获取的字符串
# 输出：按tfidf值排序的id列表

import numpy as np

from time import clock
from django.conf import settings

class CaseReasonPredict:
    def __init__(self, token):
        self.token = token

    def

if __name__ == '__main__':
    pass

